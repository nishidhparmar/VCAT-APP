<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>login</title>
</head>

<body>
    <h1>Login!</h1>
    <form action="/logindata" method="post">
        <input type="email" name="email" placeholder="email" autocomplete="off" required><br><br>
        <input type="password" name="pass" placeholder="password" autocomplete="off" required><br><br>
        <button type="submit">Submit</button>
    </form>
    <%if (error=="email is not exists" ) { %>
        <%= error %>
            <% } %>

                <%if (pass=="password is wrong" ) { %>
                    <%= pass %>
                        <% } %>
</body>

</html>